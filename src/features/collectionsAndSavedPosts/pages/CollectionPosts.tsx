import MainLayout from "../../../layouts/MainLayout";

const CollectionPosts = () => {
//   const { data: userCollections } = useGetCollections();

  return (
    <MainLayout>
      <div className="p-12">
        This is the main collection's post page
      </div>
    </MainLayout>
  );
};

export default CollectionPosts;
